# 2026-05-22

## 图片批量重命名工具 — 完成开发并部署

- 构建了纯前端图片批量重命名工具，手机端友好
- OCR 引擎：Tesseract.js v5（中文识别 chi_sim），浏览器本地处理
- 正则提取"店长：XXX"中的名字，支持手动修改识别结果
- JSZip + FileSaver 实现单文件直接下载 / 多文件 ZIP 打包
- 设计风格：杂志编辑感（Playfair Display + Noto Sans SC，奶油底 #F7EFE2 + 刊物红 #E4422E）
- 首次部署到 GitHub Pages：https://adam23.github.io/photo-rename-tool/（自定义域名 1djm.com DNS 失效导致无法访问，已弃用）
- 重新部署到新 repo：https://adam23.github.io/photo-renamer/（GitHub Pages 备用）
- 最终部署到 Vercel：https://pic-rename.vercel.app（稳定可用）
- GitHub 仓库：https://github.com/Adam23/photo-renamer
- 旧仓库：https://github.com/Adam23/photo-rename-tool（已弃用）
- 文件名规则：直接使用识别出的人名作为文件名（如 宋静霞.png），同名自动加 _1、_2 后缀
- 2026-05-22 修复：实际图片格式为「名字在前 店长在后」（如 宋静霞 店长），原来只匹配「店长在前」的正则导致识别失败。已增加双向匹配（名字在前 / 名字在后）
- 2026-05-22 强化预处理：Tesseract.js 对小字体中文识别率极低（全失败），改写了预处理 Pipeline —— 只裁剪顶部 12%（名字栏）→ 放大 3x → 二值化高对比处理，同时增加 fallback：预处理失败后用原图再试一次
- 2026-05-22 v2 重构 OCR：怀疑 CDN 语言包下载失败导致识别无效（jsdelivr 国内不稳定）。改为：1) Tesseract.js worker 配置 npmmirror 淘宝镜像加载语言数据包 2) jsdelivr CDN 加 unpkg onerror 回退 3) 二值化改为温和对比度增强（保留笔画细节）4) 加调试面板展示 OCR 原始输出文字 5) 多策略重试（原图→预处理）
- 2026-05-22 v3 重写 OCR（多策略并行 + 兜底）：Tesseract 对微信截图名字栏浅灰/蓝色文字完全无法识别。重写为 8 策略流水线：完整原图 → 裁顶8%放大 → 裁顶12%反色（浅色文字变深） → 蓝色通道增强（微信名字栏常见） → 二值化三阈值(180/200/220) → 灰度拉伸；增强「店长」模糊匹配覆盖 OCR 误读变体（位上长/电长/占长等）；增加兜底从全文提取 2-4 字中文人名。调试面板重构：显示策略命中详情和方法。部署到 pic-rename.vercel.app
- 2026-05-22 v4 右侧区域裁剪策略：用户反馈某张图片（宋静霞 店长，白色文字在红色背景上）仍未识别。分析图片发现名字文字位于头像右侧的红色卡片区域，左侧头像产生噪声。新增 preprocessRightCrop 函数：水平跳过左 30-40%（头像区域），裁剪顶部 12-15%，放大 2x，然后反色/二值化/蓝色增强处理。新增 9 条右侧裁剪策略并置于策略列表最前（优先尝试）。部署到 pic-rename.vercel.app。
